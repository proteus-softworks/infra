poof.proteus.works {
    reverse_proxy poof:4000
}

weblog.proteus.works {
    reverse_proxy weblog:4321
}

files.proteus.works {
    handle {
        root * /var/www/mysite
		file_server browse
	}
}
